this file doesn't import any engine code


Class A is the main class


The following are values in Class A:

- A table of different hash code (unknown if this is important, ask Ethan for these codes in a non-copyright way if needed)

- integer of state



The following are functions in Class A:

- A default function that sets the state

- A function that sets state to 0

- A function that adds a value byte to state

- A function that removes a value byte from state

- A function that updates the hash

- A function that returns hash value

- A function that implements the 'rol32' seen online

- A function that returns the threshold