this file doesn't import any engine code


interface A is the main interface

Class A is the main class


the following are values in Interface A:

- version number

The following are functions in Interface A:

- reads value

- writes value


The following are values in Class A:

- concurrent directory


The following are functions in Class A:

- A funciton that registers a conversion for any type

- A function that attempts to get converter type