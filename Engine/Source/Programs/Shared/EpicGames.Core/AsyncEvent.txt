this file doesn't import any engine code



Class A is the main class


The following are values in Class A:

- completion source

- task


The following are functions in Class A:

- A function that pulses an event, which allows any copies of task to continue

- A function that resets an event that is latch to an set state

- A function that moves task to completed state without having to return it to an unset state

- A function that returns true if the event is set