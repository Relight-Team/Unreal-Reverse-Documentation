This file doesn't import any engine code


Class A is the main class

The following are values in Class A:

- Task

- cancelled token task

- cancelled token source


The following are functions in Class A:

- A default function that sets the task

- A function that dispose the task

- A function that dispose the async

- A function that creates starts a new class A

- A function that runs the task

- A function that cancels the async