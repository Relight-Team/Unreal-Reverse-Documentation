this file doesn't import any engine code


Class A tells serializer to NOT use the property of the class while in serializer mode

Class B tells serializer that the class supports it

Class C tells serializer to use a certain converter for class/property


The following are values in Class C:

- type


The following are functions in Class A:

- A default function that does nothing(?)


The following are functions in Class B:

- A default function that does nothing(?)

The following are functions in Class C:

- A default function that set's the type